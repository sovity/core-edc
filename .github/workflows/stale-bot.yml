name: Close Inactive Issues

on:
  schedule:
    - cron: "30 1 * * *" # once a day (1:30 UTC)
  workflow_dispatch: # allow manual trigger

jobs:
  trigger-workflow:
    uses: sovity/core-edc-github/.github/workflows/stale-bot.yml@sovity-0.7.2.x-main
    secrets:
      envGH: ${{ secrets.GITHUB_TOKEN }}
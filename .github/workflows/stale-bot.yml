name: Close Inactive Issues

on:
  schedule:
    - cron: "30 1 * * *" # once a day (1:30 UTC)
  workflow_dispatch: # allow manual trigger

jobs:
  trigger-workflow:
    uses: sovity/core-edc-github/.github/workflows/stale-bot.yml@0.7.2_2025-01-22_1
    secrets:
      envGH: ${{ secrets.GITHUB_TOKEN }}
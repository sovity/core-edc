name: publish openapi ui

on:
  push:
    branches:
      - main

jobs:
  publish:
    uses: sovity/core-edc-github/.github/.github/workflows/publish-openapi-ui.yml@0.11.1_2025-03-10_1
    secrets: inherit

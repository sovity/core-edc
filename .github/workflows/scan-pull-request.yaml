name: Scan Pull Request

on:
  pull_request:
    branches: [ main ]
    types: [opened, edited, synchronize, reopened, labeled, unlabeled]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  trigger-workflow:
    uses: sovity/core-edc-github/.github/workflows/scan-pull-request.yml@afc78987f721e601d228b50b55e098392397f006 #0.14.0_2025-08-21
    secrets:
      envGH: ${{ secrets.GITHUB_TOKEN }}
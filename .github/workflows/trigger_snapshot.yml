name: "Publish Snapshot Build"

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  Publish-Snapshot:
    # This workflow will abort if the required secrets don't exist
    uses: sovity/core-edc-github/.github/workflows/publish-snapshot.yml@0.7.2_2025-01-22_1
    secrets: inherit
